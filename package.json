{
  "name": "jalrakshak-ai",
  "private": true,
  "description": "JalRakshak AI command center — static Vite frontend for Vercel",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
